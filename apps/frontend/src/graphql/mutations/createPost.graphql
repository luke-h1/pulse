mutation CreatePost($options: PostCreateInput!) {
  createPost(options: $options) {
    errors {
      code
      field
      message
    }
    post {
      authorId
      content
      createdAt
      id
      intro
      image
      slug
      tags
      title
      updatedAt
    }
  }
}