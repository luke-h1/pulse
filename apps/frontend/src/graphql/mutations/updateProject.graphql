mutation UpdateProject($options: ProjectUpdateInput!, $id: ID!) {
  updateProject(options: $options, id: $id) {
    errors {
      field
      message
      code
    }
    project {
      ...ProjectFragment
    }
  }
}
