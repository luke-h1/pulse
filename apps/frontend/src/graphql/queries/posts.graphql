query Posts {
  posts {
    nextCursor
    prevCursor
    results {
      id
      author {
        firstName
      }
      image
      intro
      slug
      title
      createdAt
      id
      content
      updatedAt
    }
  }
}