query Project($slug: String!) {
  project(slug: $slug) {
    appStoreUrl
    content
    createdAt
    githubUrl
    id
    intro
    playStoreUrl
    siteUrl
    slug
    title
    tags
    updatedAt
    image
    author {
      firstName
      lastName
    }
  }
}